
def random(db_connection, events):
    assert events is not None


def min_frequency_random(db_connection, events):
    assert events is not None


def min_frequency_deterministic(db_connection, events):
    assert events is not None


def frequency_weighted(db_connection, events):
    assert events is not None